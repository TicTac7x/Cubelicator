<UserControl
    x:Class="Cubelicator.View_Calibrator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Cubelicator="using:Cubelicator">

    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="100">
            <TextBlock
                FontSize="24" 
                TextAlignment="Center"
                HorizontalAlignment="Center"
                Text="{x:Bind Cubelicator:Strings.Calibrator_Description}"/>
        </Grid>

        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="400" />
            </Grid.ColumnDefinitions>

            <Grid
                x:Name="Element_Controller"
                Grid.Column="0" />

            <TextBlock
                x:Name="Element_Calibration"
                Grid.Column="1"
                Margin="50,0,100,0"
                VerticalAlignment="Center"
                FontFamily="Consolas"
                TextWrapping="NoWrap" />
            </Grid>

        <Button Grid.Row="2" HorizontalAlignment="Center" Margin="100" Click="UIEvent_FinishCalibration">
            <TextBlock
                Text="{x:Bind Cubelicator:Strings.Calibrator_FinishCalibration}"
                FontSize="24"/>
        </Button>
    </Grid>
</UserControl>
