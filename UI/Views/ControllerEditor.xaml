<UserControl
    x:Class="Cubelicator.UI.Views.ControllerEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Cubelicator.UI.Controls">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="RootProfileSelector" Grid.Row="0" HorizontalAlignment="Center" Padding="10"/>
            <Grid x:Name="ControllerRoot" Grid.Row="1" Padding="0,100" />
        </Grid>

        <Grid Grid.Column="1" Background="{x:Bind ColorButtonPressed}"
              BorderThickness="1,0,0,0" BorderBrush="{x:Bind ColorDeviceBackground}">
            <ScrollViewer
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled">

                <StackPanel
                    x:Name="RootMappedButtons"
                    Orientation="Vertical" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
