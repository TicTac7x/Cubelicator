<UserControl
    x:Class="Cubelicator.ControllerEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Cubelicator="using:Cubelicator">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                HorizontalAlignment="Stretch"
                Padding="10">
                <Grid
                    x:Name="Element_ProfileSelector"
                    HorizontalAlignment="Center"/>
            </Grid>

            <Grid x:Name="Element_Controller" Grid.Row="1" Padding="0,100" VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Column="1" Background="{x:Bind Cubelicator:Colors.ButtonPressedBackground}"
              BorderThickness="1,0,0,0" BorderBrush="{x:Bind Cubelicator:Colors.DeviceBackground}">
            <ScrollViewer
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled">

                <StackPanel
                    x:Name="Element_MappedButtons"
                    Orientation="Vertical" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
