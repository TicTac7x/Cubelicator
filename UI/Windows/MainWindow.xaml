<Window
    x:Class="Cubelicator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Cubelicator="using:Cubelicator">

    <Grid x:Name="Element_Root" Background="{x:Bind Cubelicator:Colors.AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="TitleBar"
            Grid.Row="0"
            Background="{x:Bind Cubelicator:Colors.DeviceBackground}"
            Padding="0">
            <TitleBar.IconSource>
                <FontIconSource Glyph="&#xF4AA;" />
            </TitleBar.IconSource>

            <Grid HorizontalAlignment="Stretch">
                <Grid x:Name="Element_Navigator" Grid.Row="1" HorizontalAlignment="Right" />
            </Grid>
        </TitleBar>

        <Grid Grid.Row="2">
            <Grid x:Name="Element_Dashboard" />
            <Grid x:Name="Element_ControllerEditor" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Window>
